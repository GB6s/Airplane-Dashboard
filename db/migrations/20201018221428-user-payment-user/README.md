# Migration `20201018221428-user-payment-user`

This migration has been generated by Paul Sauve at 10/18/2020, 5:14:28 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_UserPaymentInfo" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" DATETIME NOT NULL,
    "userId" INTEGER NOT NULL,
    "stripeId" TEXT NOT NULL,
    "lastFour" TEXT NOT NULL,
    "expireMonth" INTEGER NOT NULL,
    "expireYear" INTEGER NOT NULL,

    FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE
);
INSERT INTO "new_UserPaymentInfo" ("id", "createdAt", "updatedAt", "stripeId", "lastFour", "expireMonth", "expireYear") SELECT "id", "createdAt", "updatedAt", "stripeId", "lastFour", "expireMonth", "expireYear" FROM "UserPaymentInfo";
DROP TABLE "UserPaymentInfo";
ALTER TABLE "new_UserPaymentInfo" RENAME TO "UserPaymentInfo";
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201018221046-user-payment-info..20201018221428-user-payment-user
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -42,8 +42,10 @@
 model UserPaymentInfo {
   id          Int       @default(autoincrement()) @id
   createdAt   DateTime  @default(now())
   updatedAt   DateTime  @updatedAt
+  user        User      @relation(fields: [userId], references: [id])
+  userId      Int
   stripeId    String
   lastFour    String
   expireMonth Int
   expireYear  Int
```


