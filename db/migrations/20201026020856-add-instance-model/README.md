# Migration `20201026020856-add-instance-model`

This migration has been generated by Paul Sauve at 10/25/2020, 9:08:56 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "Instance" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" DATETIME NOT NULL,
    "userId" INTEGER NOT NULL,
    "apiKey" TEXT NOT NULL,
    "playerLimit" INTEGER NOT NULL,
    "cost" REAL NOT NULL,
    "expires" DATETIME NOT NULL,
    "currentPayment" TEXT NOT NULL,
    "machineId" TEXT,
    "ip" TEXT,
    "parentId" INTEGER,

    FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY ("parentId") REFERENCES "Instance"("id") ON DELETE SET NULL ON UPDATE CASCADE
)
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201018221428-user-payment-user..20201026020856-add-instance-model
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -49,4 +49,24 @@
   lastFour    String
   expireMonth Int
   expireYear  Int
 }
+
+model Instance {
+  id             Int       @default(autoincrement()) @id
+  createdAt      DateTime  @default(now())
+  updatedAt      DateTime  @updatedAt
+  user           User      @relation(fields: [userId], references: [id])
+  userId         Int
+  apiKey         String
+  playerLimit    Int
+  cost           Float
+  expires        DateTime
+  currentPayment String
+
+  // for identifying hardware
+  machineId   String?
+  ip          String?
+
+  parentId    Int?
+  parent      Instance? @relation(fields: [parentId], references: [id])
+}
```


